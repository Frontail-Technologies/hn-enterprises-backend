// Payment mode is stored as free text everywhere it's used (bill_payments,
// payments) - it's managed as master data ("Payment Types"), not a fixed
// enum, so there is nothing shared to declare here anymore.
export {};
